extends layout

block pageContent
  - var user = connection.query('select * from User where id=1;')[0];
      h1=user.Name
      img.centimg(src=user.Picture, height=200, width=200)
      p=user.Description
